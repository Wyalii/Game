import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "cdna.artstation.com",
      "www.google.com",
      "preview.redd.it",
      "s7.ezgif.com",
    ],
  },
};

export default nextConfig;
